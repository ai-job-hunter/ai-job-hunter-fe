# GitHub 创建好后，运行以下命令：

cd ai-job-hunter-agent

# 重新提交
git add -A
git commit -m "feat: initial Spring Boot Agent service

- JD analyzer, match, greet agents
- Chat reply, resume analysis, weekly report agents
- Spring AI 2.0 integration"

# 推送
git push -u origin main
